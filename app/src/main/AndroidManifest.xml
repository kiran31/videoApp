<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.binplus.exclusivevideoapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExclusiveVideoApp">
        <activity android:name=".SuccessActivity"></activity>
        <activity android:name=".OrderDetailsActivity" />
        <activity android:name=".WishlistActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".Auth.OtpValidateActivity" />
        <activity android:name=".Auth.ForgotPasswordActivity" />
        <activity android:name=".Auth.RegisterActivity" />
        <activity android:name=".Auth.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>